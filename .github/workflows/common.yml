on: [push]
name: Common checks
jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/setup-yarn
      - name: Linting
        run: yarn lint
      - name: Type checking
        run: yarn ts:check
      - name: Check formatting
        run: yarn format:check
      - name: Run unit tests
        run: yarn test:ci
